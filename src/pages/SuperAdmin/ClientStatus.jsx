import React from 'react';

const ClientStatus = () => {
    return (
        <div className="client-status-page" style={{ padding: '2rem' }}>
            <h2 style={{ color: 'var(--primary-color)', marginBottom: '1.5rem' }}>Client Status</h2>
            <p style={{ color: 'var(--text-secondary)' }}>Manage the status of all clients.</p>
        </div>
    );
};

export default ClientStatus;
